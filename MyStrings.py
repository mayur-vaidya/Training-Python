name='Mayur'
length = len(name) #This method is used for finding the length of the string and returns int value
j=0
for i in range (-1, -length-1, -1):
	print name[i],"\t",name[j],"\t"
	j+=1
for a in range (0, length):
	print name[a],

